<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.scx</groupId>
        <artifactId>scx-parent</artifactId>
        <version>1</version>
        <relativePath/>
    </parent>

    <groupId>cool.scx</groupId>
    <artifactId>scx-data-jdbc</artifactId>
    <version>4.5.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>SCX Data JDBC</name>
    <description>
        SCX Data JDBC
    </description>
    <url>https://github.com/scx-projects/scx-data-jdbc</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://github.com/scx-projects/scx-data-jdbc/blob/master/LICENSE</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>scx567888</id>
            <name>scx567888</name>
            <email>scx567888@outlook.com</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/scx-projects/scx-data-jdbc.git</connection>
        <developerConnection>scm:git:https://github.com/scx-projects/scx-data-jdbc.git</developerConnection>
        <url>https://github.com/scx-projects/scx-data-jdbc</url>
    </scm>

    <build>
        <plugins>

            <!-- 此插件用于将项目打包为 可执行 jar 包-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <!-- 因为 scx 并不是可执行 jar 包, 所以此处不添加 classpath 到描述文件中 -->
                            <addClasspath>false</addClasspath>
                        </manifest>
                    </archive>
                    <!-- 此处因为没有类似 maven-source-plugin 插件中 excludeResources 的选项 -->
                    <!-- 所以在这里手动排除资源文件, 具体文件说明见下方 -->
                    <excludes>
                        <!-- 默认 git 占位空文件 -->
                        <exclude>/.gitkeep</exclude>
                    </excludes>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <dependencies>

        <dependency>
            <groupId>dev.scx</groupId>
            <artifactId>scx-data</artifactId>
            <version>${scx-data.version}</version>
        </dependency>

        <dependency>
            <groupId>dev.scx</groupId>
            <artifactId>scx-collections</artifactId>
            <version>${scx-collections.version}</version>
        </dependency>

        <dependency>
            <groupId>cool.scx</groupId>
            <artifactId>scx-jdbc</artifactId>
            <version>${scx-jdbc.version}</version>
        </dependency>

        <dependency>
            <groupId>dev.scx</groupId>
            <artifactId>scx-reflect</artifactId>
            <version>${scx-reflect.version}</version>
        </dependency>

        <!-- ****************** 以下为测试依赖 ****************** -->

        <dependency>
            <groupId>cool.scx</groupId>
            <artifactId>scx-jdbc-spy</artifactId>
            <version>${scx-jdbc-spy.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>cool.scx</groupId>
            <artifactId>scx-jdbc-mysql</artifactId>
            <version>${scx-jdbc-mysql.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>dev.scx</groupId>
            <artifactId>scx-logging</artifactId>
            <version>${scx-logging.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- TestNG 测试包 -->
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <properties>

        <!-- *********************** 以下为依赖包版本 *********************** -->
        <scx-data.version>0.0.1</scx-data.version>
        <scx-collections.version>0.0.1</scx-collections.version>
        <scx-jdbc.version>4.5.0-SNAPSHOT</scx-jdbc.version>
        <scx-reflect.version>0.0.1</scx-reflect.version>
        <scx-jdbc-spy.version>4.5.0-SNAPSHOT</scx-jdbc-spy.version>
        <scx-jdbc-mysql.version>4.5.0-SNAPSHOT</scx-jdbc-mysql.version>
        <scx-logging.version>0.0.1</scx-logging.version>
        <testng.version>7.11.0</testng.version>

    </properties>

    <repositories>
        <repository>
            <name>Central Portal Snapshots</name>
            <id>central-portal-snapshots</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

</project>
